# Keep native bindings for ALL platforms (Windows + WSL share this node_modules).
# Without this, a yarn install from one side prunes the other side's binaries
# (rollup, esbuild, unrs-resolver, tailwind oxide) and breaks it.
--install.ignore-platform true
